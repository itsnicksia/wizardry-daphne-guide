services:
  mkdocs:
    image: squidfunk/mkdocs-material
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
    entrypoint: []
    command: ["sh", "-c", "pip install -r /docs/requirements.txt && mkdocs serve --dev-addr=0.0.0.0:8000"]
